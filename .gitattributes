# Main purpose of this file is to configure line endings for all files.
# This was taken fron .NET GitHub repo, https://github.com/dotnet/runtime/blob/423f5e1eb186b90d99002ff35725ece8cbfff30c/.gitattributes

# Set default behavior to automatically normalize line endings.
* text=auto

*.jpg  	binary
*.png 	binary
*.gif 	binary

# Force bash scripts to always use lf line endings so that if a repo is accessed
# in Unix via a file share from Windows, the scripts will work.
*.in text eol=lf
*.sh text eol=lf

# Likewise, force cmd and batch scripts to always use crlf
*.cmd text eol=crlf
*.bat text eol=crlf
